# Game Herald Rebrand Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rename the user-facing brand from `GuildRelay` to `Game Herald` (window titles, tray tooltip, README, release artifact, startup error, test-webhook content) and replace the placeholder tray icon with a heraldic pennant rendered by an in-repo icon generator.

**Architecture:** Two phases. Phase 1 is mechanical string replacement across a small set of UI/doc files; the codebase keeps `GuildRelay` as its internal codename (namespaces, assemblies, install path, code-signing cert all unchanged). Phase 2 adds `tools/IconGen/`, a tiny .NET 8 console project that uses `System.Drawing.Common` to render a swallow-tail pennant at six resolutions (16/24/32/48/64/256 px) and packs them into a multi-resolution `.ico` file written by hand using the documented ICO format. The generator runs manually; its output (the new `tray.ico`) is committed.

**Tech Stack:** .NET 8, `System.Drawing.Common` 8.0.x (Windows-only, matches the WPF host targeting). No new dependencies in the main app.

**Spec reference:** [`docs/superpowers/specs/2026-05-02-game-herald-rebrand-design.md`](../specs/2026-05-02-game-herald-rebrand-design.md)

**Branch:** `feature/game-herald-rebrand` (already created from `origin/main`).

---

## File map

### New files
- `tools/IconGen/IconGen.csproj` — console project, references `System.Drawing.Common`.
- `tools/IconGen/Program.cs` — the generator. Renders pennant, packs into ICO, writes to disk.

### Modified files
- `src/GuildRelay.App/Config/ConfigWindow.xaml` — `Title` + `<ui:TitleBar Title="…"/>`.
- `src/GuildRelay.App/Stats/StatsWindow.xaml` — `Title` + `<ui:TitleBar Title="…"/>`.
- `src/GuildRelay.App/Tray/TrayView.xaml` — `<tb:TaskbarIcon ToolTipText="…"/>`.
- `src/GuildRelay.App/App.xaml.cs` — startup-error `MessageBox.Show` text.
- `src/GuildRelay.App/Config/ConfigWindow.xaml.cs` — test-webhook `MatchedContent` string.
- `src/GuildRelay.App/Assets/tray.ico` — regenerated by IconGen.
- `README.md` — marketing prose (specific lines only; code/path references stay).
- `.github/workflows/release.yml` — ZIP artifact filename.
- `GuildRelay.sln` — add the IconGen project.

---

## Conventions

- Build: `dotnet build` (run from repo root). Expect 0 errors, 0 warnings.
- Tests: `dotnet test`. Expect 206 tests pass (no new tests in this plan; phases are UI strings + a single-purpose tool that doesn't need automated tests beyond "does it produce a non-empty .ico that the WPF app loads").
- Working directory: `C:\Users\tosha\IdeaProjects\game-event-repost`.
- Branch: `feature/game-herald-rebrand`.
- Commit per task. Use conventional-commit style (`feat`, `chore`, `docs`).

---

## Phase 1 — User-facing rename

### Task 1: Rename window titles, tray tooltip, startup error, test-webhook content

**Files:**
- Modify: `src/GuildRelay.App/Config/ConfigWindow.xaml`
- Modify: `src/GuildRelay.App/Stats/StatsWindow.xaml`
- Modify: `src/GuildRelay.App/Tray/TrayView.xaml`
- Modify: `src/GuildRelay.App/App.xaml.cs`
- Modify: `src/GuildRelay.App/Config/ConfigWindow.xaml.cs`

- [ ] **Step 1: Update `ConfigWindow.xaml`**

Find both `Title="GuildRelay — Config"` strings (one on the `<ui:FluentWindow>` element, one on `<ui:TitleBar>`). Replace with `Title="Game Herald — Config"`.

- [ ] **Step 2: Update `StatsWindow.xaml`**

Find both `Title="GuildRelay — Stats"` strings (Window title + TitleBar). Replace with `Title="Game Herald — Stats"`.

- [ ] **Step 3: Update `TrayView.xaml`**

Find `ToolTipText="GuildRelay"` and replace with `ToolTipText="Game Herald"`.

- [ ] **Step 4: Update `App.xaml.cs`**

Find the `MessageBox.Show` call in the startup catch block (around line 38-44). Replace:
```csharp
MessageBox.Show(
    $"GuildRelay failed to start:\n\n{ex.Message}\n\n{ex.GetType().Name}",
    "GuildRelay — Startup Error",
    MessageBoxButton.OK,
    MessageBoxImage.Error);
```
with:
```csharp
MessageBox.Show(
    $"Game Herald failed to start:\n\n{ex.Message}\n\n{ex.GetType().Name}",
    "Game Herald — Startup Error",
    MessageBoxButton.OK,
    MessageBoxImage.Error);
```

- [ ] **Step 5: Update `ConfigWindow.xaml.cs` test-webhook content**

In `OnTestWebhookClick` (around line 192), find:
```csharp
MatchedContent: $"GuildRelay connected - hello from {PlayerBox.Text}",
```
Replace with:
```csharp
MatchedContent: $"Game Herald connected - hello from {PlayerBox.Text}",
```

- [ ] **Step 6: Build and run all tests**

Run: `dotnet build && dotnet test`
Expected: 0 build errors. 206 tests pass.

- [ ] **Step 7: Commit**

```bash
git add -u src/GuildRelay.App
git commit -m "feat(app): rename user-facing 'GuildRelay' to 'Game Herald' in UI strings"
```

### Task 2: Rename README marketing prose

**Files:**
- Modify: `README.md`

The README has 17+ `GuildRelay` references mixed between marketing prose (rename) and code/path strings (keep). The list of occurrences was identified during spec review:

**Lines to rename to "Game Herald":**
- Line 1: `# GuildRelay` → `# Game Herald`
- Line 7: `GuildRelay watches your game session` → `Game Herald watches your game session`
- Line 13: `GuildRelay-win-x64.zip` → `GameHerald-win-x64.zip`
- Line 21: `GuildRelay runs quietly` → `Game Herald runs quietly`
- Line 55: `what GuildRelay is doing every capture interval` → `what Game Herald is doing every capture interval`
- Line 67: `GuildRelay recognizes all MO2 chat channels` → `Game Herald recognizes all MO2 chat channels`
- Line 124: `**GuildRelay does not read…**` → `**Game Herald does not read…**`
- Line 126: `GuildRelay is designed from the ground up` → `Game Herald is designed from the ground up`
- Line 128 (table header): `What GuildRelay does` → `What Game Herald does`
- Line 137: `What GuildRelay explicitly does NOT do:` → `What Game Herald explicitly does NOT do:`
- Line 146: `GuildRelay captures what your monitor shows` → `Game Herald captures what your monitor shows`
- Line 159: `GuildRelay automatically joins adjacent lines` → `Game Herald automatically joins adjacent lines`
- Line 217: `GuildRelay communicates only with Discord` → `Game Herald communicates only with Discord`

**Lines that DO NOT change** (these reference internal codename / file paths / project names, all of which the spec keeps as `GuildRelay`):
- Line 14: `Run \`GuildRelay.App.exe\`.` (executable filename)
- Line 150: `%APPDATA%\GuildRelay\config.json`, `%APPDATA%\GuildRelay\logs\` (data dir)
- Line 183: `dotnet publish src/GuildRelay.App` (code path)
- Line 186: `src/GuildRelay.App/bin/.../publish/GuildRelay.App.exe` (code path)
- Lines 191–200: project-structure block (`GuildRelay.sln`, `GuildRelay.Core/`, etc.) — code-level

(Note: line numbers may shift slightly as you edit. Use the prose context to find each occurrence.)

- [ ] **Step 1: Apply the renames listed above to `README.md`**

Open `README.md`. Apply each of the 13 replacements. Preserve the 6 lines that should not change.

- [ ] **Step 2: Verify**

Run:
```bash
grep -n "GuildRelay" README.md
```
Expected output: only the 6 "stays" lines (14, 150, 183, 186, and the project-structure block 191-200). All marketing prose should now say "Game Herald".

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs(readme): rename user-facing 'GuildRelay' to 'Game Herald' in marketing prose"
```

### Task 3: Rename release workflow artifact

**Files:**
- Modify: `.github/workflows/release.yml`

- [ ] **Step 1: Update both ZIP filename references**

Find lines 63 + 68 (approximately):
```yaml
Compress-Archive -Path "$publishDir/*" -DestinationPath "GuildRelay-${{ github.ref_name }}-win-x64.zip"
```
and
```yaml
files: GuildRelay-${{ github.ref_name }}-win-x64.zip
```

Replace both `GuildRelay-` prefixes with `GameHerald-`:
```yaml
Compress-Archive -Path "$publishDir/*" -DestinationPath "GameHerald-${{ github.ref_name }}-win-x64.zip"
```
```yaml
files: GameHerald-${{ github.ref_name }}-win-x64.zip
```

The `signtool.exe sign … "$publishDir\GuildRelay.App.exe"` reference (around line 54) and `Copy-Item "certs/GuildRelay-CodeSigning.cer" …` (line 62) **stay unchanged** — they reference the assembly executable and the code-signing cert filename, both of which the spec keeps as the `GuildRelay` codename.

- [ ] **Step 2: Verify**

Run:
```bash
grep -n "GuildRelay\|GameHerald" .github/workflows/release.yml
```
Expected: the two ZIP filename lines say `GameHerald-…`. The `GuildRelay.App.exe` and `GuildRelay-CodeSigning.cer` lines remain.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/release.yml
git commit -m "chore(release): rename ZIP artifact GuildRelay-* to GameHerald-*"
```

---

## Phase 2 — Icon generator tool

### Task 4: Scaffold `tools/IconGen` project

**Files:**
- Create: `tools/IconGen/IconGen.csproj`
- Create: `tools/IconGen/Program.cs` (skeleton)
- Modify: `GuildRelay.sln`

- [ ] **Step 1: Create the csproj**

`tools/IconGen/IconGen.csproj`:
```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <RootNamespace>IconGen</RootNamespace>
    <AssemblyName>IconGen</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.Drawing.Common" Version="8.0.10" />
  </ItemGroup>

</Project>
```

- [ ] **Step 2: Create skeleton `Program.cs`**

`tools/IconGen/Program.cs`:
```csharp
namespace IconGen;

public static class Program
{
    public static int Main(string[] args)
    {
        System.Console.WriteLine("IconGen scaffold OK");
        return 0;
    }
}
```

- [ ] **Step 3: Add to solution**

Run from repo root:
```bash
dotnet sln add tools/IconGen/IconGen.csproj
```

- [ ] **Step 4: Build full solution**

Run: `dotnet build`
Expected: 0 errors, 0 warnings. The new `IconGen.dll` and `IconGen.exe` are produced under `tools/IconGen/bin/Debug/net8.0-windows/`.

- [ ] **Step 5: Run the skeleton**

Run: `dotnet run --project tools/IconGen`
Expected output: `IconGen scaffold OK`.

- [ ] **Step 6: Commit**

```bash
git add tools/IconGen/IconGen.csproj tools/IconGen/Program.cs GuildRelay.sln
git commit -m "feat(tools): scaffold IconGen console project for tray icon generation"
```

### Task 5: Implement pennant rendering + ICO writer

**Files:**
- Modify: `tools/IconGen/Program.cs`

- [ ] **Step 1: Replace `Program.cs` with the full implementation**

`tools/IconGen/Program.cs`:
```csharp
using System;
using System.Collections.Generic;
using System.Drawing;
using System.Drawing.Drawing2D;
using System.Drawing.Imaging;
using System.IO;

namespace IconGen;

/// <summary>
/// Renders a swallow-tail heraldic pennant at six resolutions (16/24/32/48/64/256 px)
/// and packs them into a multi-resolution .ico file. Run manually when the icon design
/// changes; the resulting .ico is committed to the repo.
///
/// Default output path: `src/GuildRelay.App/Assets/tray.ico` (relative to the working
/// directory — run from the repo root). Override with a CLI argument:
///     dotnet run --project tools/IconGen -- some/other/path.ico
/// </summary>
public static class Program
{
    private static readonly int[] Sizes = { 16, 24, 32, 48, 64, 256 };
    private static readonly Color GoldColor = Color.FromArgb(0xFF, 0xD4, 0xAF, 0x37);
    private const string DefaultOutputPath = "src/GuildRelay.App/Assets/tray.ico";

    public static int Main(string[] args)
    {
        var outputPath = args.Length > 0 ? args[0] : DefaultOutputPath;

        var pngBuffers = new List<byte[]>(Sizes.Length);
        foreach (var size in Sizes)
        {
            using var bitmap = RenderPennant(size);
            using var ms = new MemoryStream();
            bitmap.Save(ms, ImageFormat.Png);
            pngBuffers.Add(ms.ToArray());
        }

        var fullPath = Path.GetFullPath(outputPath);
        Directory.CreateDirectory(Path.GetDirectoryName(fullPath)!);
        WriteIco(fullPath, pngBuffers);

        Console.WriteLine($"Wrote {Sizes.Length} resolutions to {fullPath}");
        return 0;
    }

    /// <summary>
    /// Renders the pennant onto an NxN bitmap with a transparent background.
    /// Pole at 25% canvas-x, full height. Pennant attached at the pole's top,
    /// flying right with a triangular V-notch in its fly end.
    /// </summary>
    private static Bitmap RenderPennant(int size)
    {
        var bitmap = new Bitmap(size, size, PixelFormat.Format32bppArgb);
        using var g = Graphics.FromImage(bitmap);

        g.CompositingMode = CompositingMode.SourceCopy;
        g.Clear(Color.Transparent);
        g.CompositingMode = CompositingMode.SourceOver;
        // Anti-alias only at sizes large enough to benefit. At 16x16, sharp edges
        // read better than smudged ones.
        g.SmoothingMode = size >= 32 ? SmoothingMode.AntiAlias : SmoothingMode.None;

        int poleWidth = Math.Max(1, size / 16);
        int poleX = size / 4;

        using var goldBrush = new SolidBrush(GoldColor);

        // Pole: full canvas height
        g.FillRectangle(goldBrush, poleX, 0, poleWidth, size);

        // Pennant: 5-point swallow-tail polygon attached to top of pole
        int pennantWidth = (int)Math.Round(size * 0.65);
        int pennantHeight = (int)Math.Round(size * 0.35);
        int pennantTop = Math.Max(1, size / 16);
        int notchDepth = Math.Max(1, pennantWidth / 4);

        var attachX = poleX + poleWidth;
        var flyX = attachX + pennantWidth;
        var pennantPoints = new[]
        {
            new Point(attachX, pennantTop),                                  // top-left
            new Point(flyX, pennantTop),                                     // top-right (fly top)
            new Point(flyX - notchDepth, pennantTop + pennantHeight / 2),    // V-notch tip
            new Point(flyX, pennantTop + pennantHeight),                     // bottom-right (fly bottom)
            new Point(attachX, pennantTop + pennantHeight),                  // bottom-left
        };
        g.FillPolygon(goldBrush, pennantPoints);

        return bitmap;
    }

    /// <summary>
    /// Writes a multi-resolution .ico file. ICO format reference:
    /// ICONDIR header (6 bytes) + ICONDIRENTRY[] (16 bytes each) + image data.
    /// We embed each image as a PNG (modern .ico format supports this and it
    /// keeps the writer simple — no need for separate BMP encoding).
    /// </summary>
    private static void WriteIco(string path, List<byte[]> pngs)
    {
        if (pngs.Count != Sizes.Length)
            throw new ArgumentException($"Expected {Sizes.Length} PNG buffers, got {pngs.Count}");

        using var fs = File.Create(path);
        using var bw = new BinaryWriter(fs);

        // ICONDIR (6 bytes)
        bw.Write((ushort)0);             // reserved
        bw.Write((ushort)1);             // type = icon (.ico)
        bw.Write((ushort)pngs.Count);    // count

        // ICONDIRENTRY[] (16 bytes each)
        int dataOffset = 6 + pngs.Count * 16;
        for (int i = 0; i < pngs.Count; i++)
        {
            int size = Sizes[i];
            byte width = size >= 256 ? (byte)0 : (byte)size;  // 0 means 256 in ICO
            byte height = width;
            bw.Write(width);
            bw.Write(height);
            bw.Write((byte)0);              // colorCount = 0 (not palettized)
            bw.Write((byte)0);              // reserved
            bw.Write((ushort)1);            // planes
            bw.Write((ushort)32);           // bitCount (BGRA)
            bw.Write((uint)pngs[i].Length); // bytesInRes
            bw.Write((uint)dataOffset);     // imageOffset
            dataOffset += pngs[i].Length;
        }

        // Image data, in the same order as the directory entries
        foreach (var png in pngs)
            bw.Write(png);
    }
}
```

- [ ] **Step 2: Build**

Run: `dotnet build tools/IconGen/IconGen.csproj`
Expected: 0 errors, 0 warnings.

- [ ] **Step 3: Run**

Run from repo root:
```bash
dotnet run --project tools/IconGen
```
Expected output: `Wrote 6 resolutions to /full/path/to/src/GuildRelay.App/Assets/tray.ico`.

- [ ] **Step 4: Verify the .ico is non-empty and reasonable**

Run:
```bash
ls -la src/GuildRelay.App/Assets/tray.ico
```
Expected: a non-zero file size, typically in the range 5-15 KB (most of the bytes are the 256x256 PNG payload).

- [ ] **Step 5: Open the .ico in a viewer to sanity-check the design**

Open `src/GuildRelay.App/Assets/tray.ico` in Windows Explorer's preview pane, or in any image viewer that supports ICO. Confirm:
- Visible gold pennant on a transparent background.
- All six resolutions are present (most viewers show a list).
- At 16×16, the pennant silhouette is recognisable (swallow-tail shape distinguishable).

If the design needs adjustment (e.g., pennant too small, pole too thin), tweak the proportions in `RenderPennant` and re-run. Adjustments are version-controlled diffs to `Program.cs`.

- [ ] **Step 6: Commit**

```bash
git add tools/IconGen/Program.cs src/GuildRelay.App/Assets/tray.ico
git commit -m "feat(tools): IconGen renders heraldic pennant + replace placeholder tray.ico"
```

---

## Phase 3 — Manual smoke test

### Task 6: Verify end-to-end

**Files:** none modified — runtime check.

- [ ] **Step 1: Build and run all tests**

Run: `dotnet build && dotnet test`
Expected: 0 errors. 206 tests pass.

- [ ] **Step 2: Run the app**

Run: `dotnet run --project src/GuildRelay.App`

- [ ] **Step 3: Verify each user-facing rename**

- Tray icon (system tray, bottom-right of taskbar by default): hover → tooltip says "Game Herald".
- Tray icon visual: shows the new gold pennant, not the old placeholder.
- Config window opens automatically. Title bar reads "Game Herald — Config".
- Open Stats window from tray. Title reads "Game Herald — Stats".
- (If you want to verify the test-webhook string: paste a webhook URL on the Settings tab, click "Test webhook", check the Discord channel for `Game Herald connected - hello from {your-name}`.)

- [ ] **Step 4: Verify code-level identifiers are unchanged**

Run:
```bash
grep -rn "GuildRelay" src --include='*.csproj' --include='*.cs' --include='*.xaml' | wc -l
```
Expected: roughly the same number as before this branch (only namespaces, project references, and the `%APPDATA%\GuildRelay\` data dir reference remain — no user-facing strings).

Also confirm the data dir is unchanged:
```bash
grep -n "GuildRelay" src/GuildRelay.App/CoreHost.cs
```
Expected: the `Path.Combine(... , "GuildRelay")` data-dir line is intact (around line 57).

- [ ] **Step 5: No commit needed** unless smoke-test surfaced a defect. If it did, fix the defect inline and commit.

---

## Self-review checklist (post-write)

- [ ] Spec §1 Goal → implemented across Tasks 1-5 ✓
- [ ] Spec §3.1 What changes → Tasks 1, 2, 3 ✓
- [ ] Spec §3.2 What does not change → covered by what the tasks DO NOT touch (verified by Task 6 step 4) ✓
- [ ] Spec §4.1 Logo concept → Task 5 step 1 (`RenderPennant`) ✓
- [ ] Spec §4.2 Visual spec (sizes, colours, transparent bg, no anti-alias <32px) → Task 5 step 1 ✓
- [ ] Spec §4.3 Implementation tooling → Tasks 4 + 5 ✓
- [ ] Spec §4.4 Why in-repo → covered narratively in plan header ✓
- [ ] Spec §5 Out of scope → no task touches code-level identifiers, marketing logo at 1024 px, animated variants, historical specs, cert reissuance ✓
- [ ] Spec §6 Anti-cheat → no new capture/audio/process work; nothing touches MO2 ✓
