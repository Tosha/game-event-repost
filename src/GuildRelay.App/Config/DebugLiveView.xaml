<ui:FluentWindow x:Class="GuildRelay.App.Config.DebugLiveView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Chat Watcher — Live Debug View" Width="700" Height="600"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        Closing="OnClosing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Chat Watcher — Live Debug View"/>

        <Grid Grid.Row="1" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="180"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Captured image -->
            <TextBlock Grid.Row="0" Text="Captured region (raw)" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <Border Grid.Row="1" BorderBrush="{ui:ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="1" Margin="0,0,0,8">
                <Image x:Name="CaptureImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
            </Border>

            <!-- OCR lines + channels -->
            <TextBlock Grid.Row="2" Text="OCR output → parsed channel → normalized" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox Grid.Row="3" x:Name="OcrOutputBox" IsReadOnly="True"
                     FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" Margin="0,0,0,8"
                     Background="{ui:ThemeResource ControlFillColorDefaultBrush}"/>

            <!-- Match results -->
            <TextBox Grid.Row="4" x:Name="MatchBox" IsReadOnly="True" Height="80"
                     FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Background="{ui:ThemeResource ControlFillColorDefaultBrush}"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
