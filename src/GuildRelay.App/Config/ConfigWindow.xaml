<ui:FluentWindow x:Class="GuildRelay.App.Config.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:GuildRelay.App.Config"
        Title="GuildRelay — Config" Width="600" Height="560"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="GuildRelay — Config"/>

        <TabControl Grid.Row="1" Margin="12,4,12,12" x:Name="MainTabs">
            <TabItem x:Name="ChatTabItem">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="ChatDot" Text="" Foreground="#44FF44" Margin="0,0,4,0" FontSize="10" VerticalAlignment="Center"/>
                        <TextBlock Text="Chat Watcher"/>
                    </StackPanel>
                </TabItem.Header>
                <local:ChatConfigTab x:Name="ChatTab"/>
            </TabItem>
            <TabItem x:Name="AudioTabItem">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="AudioDot" Text="" Foreground="#44FF44" Margin="0,0,4,0" FontSize="10" VerticalAlignment="Center"/>
                        <TextBlock Text="Audio Watcher"/>
                    </StackPanel>
                </TabItem.Header>
                <local:AudioConfigTab x:Name="AudioTab"/>
            </TabItem>
            <TabItem x:Name="StatusTabItem">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="StatusDot" Text="" Foreground="#44FF44" Margin="0,0,4,0" FontSize="10" VerticalAlignment="Center"/>
                        <TextBlock Text="Status Watcher"/>
                    </StackPanel>
                </TabItem.Header>
                <local:StatusConfigTab x:Name="StatusTab"/>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Settings24" Margin="0,0,6,0" FontSize="14"/>
                        <TextBlock Text="Settings"/>
                    </StackPanel>
                </TabItem.Header>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12">
                        <TextBlock Text="Discord webhook URL" FontWeight="SemiBold"/>
                        <PasswordBox x:Name="WebhookBox" Height="32" Margin="0,4,0,12"/>
                        <TextBlock Text="Player name" FontWeight="SemiBold"/>
                        <TextBox x:Name="PlayerBox" Height="32" Margin="0,4,0,12"/>
                        <StackPanel Orientation="Horizontal">
                            <ui:Button Content="Test webhook" Click="OnTestWebhookClick" Appearance="Primary" Margin="0,0,8,0"/>
                            <ui:Button Content="Save"         Click="OnSaveClick"        Margin="0,0,8,0"/>
                            <ui:Button Content="Close"        Click="OnCloseClick"/>
                        </StackPanel>
                        <TextBlock x:Name="StatusText" Margin="0,12,0,0" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>

                        <Separator Margin="0,16,0,12"/>

                        <TextBlock Text="Chat Watcher" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>

                        <TextBlock Text="Capture interval (seconds)" FontWeight="SemiBold"/>
                        <TextBox x:Name="IntervalBox" Width="120" HorizontalAlignment="Left" Margin="0,4,0,8"/>

                        <TextBlock Text="OCR confidence threshold (0.0 - 1.0)" FontWeight="SemiBold"/>
                        <TextBox x:Name="ConfidenceBox" Width="120" HorizontalAlignment="Left" Margin="0,4,0,8"/>

                        <TextBlock Text="Default rule cooldown (seconds)" FontWeight="SemiBold"/>
                        <TextBox x:Name="CooldownBox" Width="120" HorizontalAlignment="Left" Margin="0,4,0,4"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</ui:FluentWindow>
