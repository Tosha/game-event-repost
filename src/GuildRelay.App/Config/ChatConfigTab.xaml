<UserControl x:Class="GuildRelay.App.Config.ChatConfigTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="12">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <ui:ToggleSwitch x:Name="EnabledToggle" IsChecked="False" Checked="OnToggleChanged" Unchecked="OnToggleChanged"/>
                <TextBlock Text="Chat Watcher" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="14" Margin="8,0,0,0"/>
                <ui:Button Content="Live View" Click="OnOpenLiveView" Margin="16,0,0,0" FontSize="11"/>
            </StackPanel>

            <TextBlock Text="Chat region" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                <ui:Button Content="Pick region" Click="OnPickRegion" Margin="0,0,8,0"/>
                <TextBlock x:Name="RegionLabel" VerticalAlignment="Center"
                           Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                           Text="No region selected"/>
            </StackPanel>

            <TextBlock Text="Capture interval (ms)" FontWeight="SemiBold"/>
            <TextBox x:Name="IntervalBox" Width="120" HorizontalAlignment="Left" Margin="0,4,0,8"/>

            <TextBlock Text="OCR confidence threshold (0.0 - 1.0)" FontWeight="SemiBold"/>
            <TextBox x:Name="ConfidenceBox" Width="120" HorizontalAlignment="Left" Margin="0,4,0,8"/>

            <TextBlock Text="Default rule cooldown (seconds)" FontWeight="SemiBold"/>
            <TextBox x:Name="CooldownBox" Width="120" HorizontalAlignment="Left" Margin="0,4,0,12"/>

            <!-- Rule templates -->
            <TextBlock Text="Rule templates" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Margin="0,4,0,12">
                <ComboBox x:Name="TemplateCombo" Width="220" Margin="0,0,8,0"/>
                <ui:Button Content="Load Template" Click="OnLoadTemplate"/>
            </StackPanel>

            <!-- Active rules list -->
            <TextBlock Text="Active rules" FontWeight="SemiBold"/>
            <ListBox x:Name="RulesList" Height="100" Margin="0,4,0,8"
                     SelectionChanged="OnRuleSelected"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <ui:Button Content="Remove Selected" Click="OnRemoveRule" Margin="0,0,8,0"/>
            </StackPanel>

            <!-- Rule editor -->
            <TextBlock Text="Rule editor" FontWeight="SemiBold" FontSize="13"/>
            <TextBlock Text="Label" Margin="0,4,0,0"/>
            <TextBox x:Name="RuleLabelBox" Width="300" HorizontalAlignment="Left" Margin="0,2,0,4"/>

            <TextBlock Text="Channels" Margin="0,4,0,0"/>
            <WrapPanel x:Name="ChannelPanel" Margin="0,2,0,4"/>

            <TextBlock Text="Keywords (comma-separated)" Margin="0,4,0,0"/>
            <TextBox x:Name="KeywordsBox" Height="50" Margin="0,2,0,4"
                     TextWrapping="Wrap" AcceptsReturn="False" FontFamily="Consolas"
                     VerticalScrollBarVisibility="Auto"/>

            <StackPanel Orientation="Horizontal" Margin="0,2,0,4">
                <RadioButton x:Name="ContainsAnyRadio" Content="Contains any keyword" IsChecked="True" Margin="0,0,16,0"/>
                <RadioButton x:Name="RegexRadio" Content="Regex"/>
            </StackPanel>

            <TextBlock Text="Cooldown (seconds)" Margin="0,4,0,0"/>
            <TextBox x:Name="RuleCooldownBox" Width="120" HorizontalAlignment="Left" Margin="0,2,0,8"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <ui:Button Content="Add Rule" Click="OnAddRule" Appearance="Primary" Margin="0,0,8,0"/>
                <ui:Button Content="Update Selected" Click="OnUpdateRule" Margin="0,0,8,0"/>
            </StackPanel>

            <ui:Button Content="Save Chat Settings" Click="OnSave" Appearance="Primary"
                       HorizontalAlignment="Left"/>

            <!-- Test message -->
            <TextBlock Text="Test a message against your rules" FontWeight="SemiBold" Margin="0,16,0,0"/>
            <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                <TextBox x:Name="TestMessageBox" Width="350" Margin="0,0,8,0" FontFamily="Consolas"/>
                <ui:Button Content="Test" Click="OnTestMessage"/>
            </StackPanel>
            <TextBlock x:Name="TestResultText" Margin="0,4,0,0"
                       Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
            <TextBlock x:Name="StatusText" Margin="0,8,0,0"
                       Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
